/**
 * Thin facade over cover palette hooks — no duplicate extraction logic.
 */
export { useCoverPalette, paletteToCssVars, DEFAULT_PALETTE, isMotionCoverMedia, isVideoCoverFile } from "@/hooks/useCoverPalette";
