// src/lib/auth/constants.js (full isAdminUser — Step 1)

export const ADMIN_USER_ID = "545cd959-5cae-4009-8a91-1c46fe2f4d27";
export const ADMIN_EMAIL = "book2mrrw@gmail.com";

export function isAdminUser(user) {
  if (!user) return false;
  if (user.id === ADMIN_USER_ID) return true;
  if (user.role === "admin") return true;
  const adminEmail = ADMIN_EMAIL.toLowerCase();
  for (const candidate of [user.email, user.authEmail]) {
    const email = String(candidate || "").trim().toLowerCase();
    if (email && email === adminEmail) return true;
  }
  return false;
}
