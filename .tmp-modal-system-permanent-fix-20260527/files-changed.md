# Files changed

| File | Change |
|------|--------|
| `src/components/preview/ImmersivePreviewModal.js` | Fix credit row iteration (root throw); album track selection syncs queue via `onPlayTrackAtIndex`; stable id comparison |
| `src/app/page.js` | Modal mutual dismissal; album `AnimatePresence`; `playAlbumModalTrackAtIndex`; suppress mini-player during album modal; safe album price; stable single keys; remove unused access memos |
| `src/components/home/FeaturesRail.js` | Guard `price` before `toFixed`; stable list keys |
